# Wave 6 Audit Findings — Effects, Werkstatt, Export

Source-verified against the installed SDK (`studio-sdk@0.0.154`) sub-packages, tag-pinned in the
openDAW checkout: `studio-core@0.0.152`, `studio-adapters@0.0.116`, `studio-boxes@0.0.94`
(schema via `forge-boxes` at that tag), `lib-dsp@0.0.84`, `lib-std@0.0.78`, plus
`node_modules/@opendaw/*/dist` as installed truth. Evidence cites `tag:path` (openDAW checkout)
or `dist:path` (node_modules).

Verdicts: **confirmed** / **nuanced** (true in spirit, detail off or caveat missing) / **wrong**.

---

## 1. Verdict table — `src/demos/effects/CLAUDE.md`

| # | Claim | Verdict | Evidence | Action |
|---|-------|---------|----------|--------|
| E1 | `insertEffect()` returns `EffectBox` union; cast directly | confirmed | dist:studio-core/dist/EffectBox.d.ts (23-box union); dist:studio-core/dist/project/ProjectApi.d.ts:52 | None. Note: signature is `insertEffect(field, factory, insertIndex?: int)` — the optional `insertIndex` is undocumented and is the fix for finding D3. Add to CLAUDE.md. |
| E2 | `ScriptCompiler.create({headerTag, registryName, functionName})` | confirmed | dist:studio-adapters/dist/ScriptCompiler.d.ts:12-17 | None |
| E3 | `compile(audioContext, editing, deviceBox, source)` / `load(audioContext, deviceBox)` / `stripHeader(source)` | nuanced | dist:ScriptCompiler.d.ts:18-20 — `compile` has a 5th optional param `append?: boolean` not documented | Optional: add `append?` to CLAUDE.md |
| E4 | Header write-back `// @werkstatt js 1 <update-number>` | confirmed | dist:ScriptCompiler.js `createHeader = (update) => \`// @${tag} js ${COMPILER_VERSION} ${update}\n\``, `COMPILER_VERSION = 1` | None |
| E5 | "Without compilation, the processor sees `update === 0` and stays silent" | nuanced | @opendaw/studio-core@0.0.152:packages/studio/core-processors/src/devices/audio-effects/WerkstattDeviceProcessor.ts:17 — processor-side `parseUpdate` returns **-1** (not 0) for headerless code and gates on `newUpdate > 0`. The `0` value is the *client-side* `ScriptCompiler.parseHeader` convention (`load()` early-returns at 0). Net behavior (silent without compile) is correct. | Reword CLAUDE.md: "processor never loads a user processor for headerless code (its parse yields -1; the compiler-side parse yields 0)" |
| E6 | `block.flags & 4` = playing flag | confirmed | @opendaw/studio-core@0.0.152:packages/studio/core-processors/src/processing.ts:5-13 — `BlockFlag { transporting=1<<0, discontinuous=1<<1, playing=1<<2 (=4), bpmChanged=1<<3 }` | None. Optionally document `transporting=1` vs `playing=4` distinction |
| E7 | SDK does NOT clear output buffers between blocks | confirmed | WerkstattDeviceProcessor.ts `processAudio()` never clears `#output` before `proc.process()`; clears only in `reset()`/`#silence()` | None. Bonus found: `validateOutput` silences the device (with `deviceMessage` to client) on NaN or \|sample\| > 1000 (~60 dB) — worth adding to CLAUDE.md |
| E8 | `ScriptDeclaration.parseGroups()` → `DeclarationSection[]`, `group: {label, color} \| null` | confirmed | dist:studio-adapters/dist/ScriptDeclaration.d.ts:28-31,38 (`Nullable<GroupDeclaration>`) | None |
| E9 | Werkstatt params via `parameters.pointerHub.incoming()` → `WerkstattParameterBox` with `.label`/.`value`/.`defaultValue` | confirmed | @opendaw/studio-boxes@0.0.94:packages/studio/forge-boxes/src/schema/devices/audio-effects/WerkstattParameterBox.ts (fields 2 label string, 4 value float32 + ParameterPointerRules, 5 defaultValue float32; plus owner pointer + index) | None |
| E10 | Compressor parameter table (12 rows) | confirmed | @opendaw/studio-adapters@0.0.116:packages/studio/adapters/src/devices/audio-effects/CompressorDeviceBoxAdapter.ts `#wrapParameters` — lookahead/automakeup/autoattack/autorelease bool; inputgain linear ±30; threshold linear −60..0; ratio exponential 1..24; knee linear 0..24; attack linear 0..100 ms; release linear 5..1500 ms; makeup linear ±40; mix unipolar | None — every row matches exactly |
| E11 | Crusher `crush` inverted: higher value = MORE crushing | confirmed | @opendaw/studio-core@0.0.152:…/CrusherDeviceProcessor.ts `setCrush(1.0 - value)`; @opendaw/lib-dsp@0.0.84:packages/lib/dsp/src/crusher.ts `setCrush` maps exponentially 20 Hz → nyquist, so box value 1 → 20 Hz crushed rate = max crushing | None |
| E12 | StereoTool stereo width bipolar −1..1, NOT 0-2 | confirmed | StereoToolDeviceBoxAdapter.ts `ValueMapping.bipolar()`; schema `constraints: "bipolar"` | None |
| E13 | Tidal 17-entry RateFractions | confirmed | TidalDeviceBoxAdapter.ts:14-19 — counted 17 entries (1/1 … 1/128), order matches the demo's hard-coded list exactly | None |
| E14 | Delay 21-entry Fractions (Off→1/1) | confirmed | DelayDeviceBoxAdapter.ts:14-20 — counted 21; index 14 = 1/4, 13 = 3/16, 11 = 1/8 (demo mapping correct) | None |
| E15 | Dattorro preDelay ms 0–1000; wet/dry DefaultDecibel | confirmed | DattorroReverbDeviceBoxAdapter.ts `ValueMapping.linear(0,1000)` + schema unit "ms"; wet/dry `ValueMapping.DefaultDecibel` | None |
| E16 | Reverb adapter — "wet, dry, roomSize, damping, width" | **wrong** | ReverbDeviceBoxAdapter.ts `#wrapParameters`: actual params are `decay` (UI label "Room-Size", unipolar), `preDelay` (exponential 0.001–0.5 **s**), `damp` (unipolar), `filter` (bipolar), `dry`, `wet` (DefaultDecibel). No `roomSize` or `width` field exists | Fix CLAUDE.md line: list decay/preDelay/damp/filter/dry/wet |
| E17 | Gate adapter — "threshold, attack, hold, release, range" | **wrong** | GateDeviceBoxAdapter.ts: actual params `inverse` (bool), `threshold` (−80..0), `return` (0..24 dB), `attack` (0..1000 ms), `hold` (0..500 ms), `release` (1..2000 ms), `floor` (decibel −72,−12,0). No `range` param | Fix CLAUDE.md line |
| E18 | Maximizer adapter — "ceiling, release" | **wrong** | MaximizerDeviceBoxAdapter.ts wraps only `threshold` (UI mapping linear −24..0); box schema (forge-boxes MaximizerDeviceBox.ts) has `lookahead` (bool) + `threshold` (constraints −30..0 dB). No ceiling/release fields | Fix CLAUDE.md line: lookahead + threshold; note schema −30..0 vs adapter UI −24..0 |
| E19 | Modular system: `.modules`, `.connections`, `catchupAndSubscribe(ModularSystemListener)` | confirmed | dist:studio-adapters/dist/modular/modular.d.ts:16-26; ModularDeviceBoxAdapter exposes `.modular(): ModularAdapter` | None |
| E20 | MIDI effect adapters list (Arpeggio, Pitch, Velocity, Spielwerk, Zeitgeist) | confirmed | dist:studio-adapters/dist/devices/midi-effects/ — all five present | None |
| E21 | WavFile in lib-dsp, 24-bit PCM decode support | confirmed | dist:lib-dsp/dist/wav-file.js:60 (`audioFormat === 1 && bitsPerSample === 24`) | None |
| E22 | Apparat/Werkstatt/Spielwerk factories + box types | confirmed | dist:studio-core/dist/EffectFactories.d.ts (Werkstatt, Spielwerk); dist:studio-adapters/dist/factories/InstrumentFactories.d.ts:21 (Apparat). Note `InstrumentFactories` lives in **studio-adapters**, not studio-core | None (package note optional) |
| E23 | Effect display-name changes (0.0.129 history) | not re-verified | Historical claim predating the audit range | None |

## 2. Verdict table — `src/demos/export/CLAUDE.md`

| # | Claim | Verdict | Evidence | Action |
|---|-------|---------|----------|--------|
| X1 | `countStems(Option.None)` returns 1 → mixdown branch, metronome included | confirmed | dist:studio-adapters/dist/EngineProcessorAttachment.js — `none: () => 1`, and config-without-stems also → 1; @opendaw/studio-core@0.0.152:…/EngineProcessor.ts:395-404 — `stemExports.length === 0` branch mixes `#metronome.output.mixInto(mainOutput)`; stem branch writes per-unit stereo pairs, no metronome | None |
| X2 | Stems record shape incl. `skipChannelStrip`, `range` | confirmed | dist:EngineProcessorAttachment.d.ts:12-26 — `ExportStemConfiguration {includeAudioEffects, includeSends, useInstrumentOutput, skipChannelStrip?, fileName}`; `ExportRange = "full" \| {start, end}` | None, but see X3/X4 |
| X3 | CLAUDE.md stem example sets `useInstrumentOutput: true` with comment "render with effects" | **wrong** | @opendaw/studio-core@0.0.152:packages/studio/core-processors/src/AudioDeviceChain.ts:152-160 — when `useInstrumentOutput`, the chain wires instrument output directly to the bus and returns; effects, sends, AND channel strip are all bypassed (`AudioUnit.ts:56` returns `input.audioOutput`). `includeAudioEffects: true` is then dead. openDAW's own export dialog deliberately omits the flag (@opendaw/studio-core@0.0.152:packages/app/studio/src/project/ProjectDialogs.tsx:118-127, explicit comment) | Fix CLAUDE.md example: omit `useInstrumentOutput` (or set false) when effects are wanted; document the bypass semantics |
| X4 | `range?` comment placed inside config passed to `worklets.createEngine` | nuanced | `range` is consumed ONLY by `OfflineEngineRenderer` (@opendaw/studio-core@0.0.152:packages/studio/core/src/OfflineEngineRenderer.ts:222). EngineProcessor.ts has no `range` reference — the manual `createEngine` path ignores it | CLAUDE.md should state range works only via OfflineEngineRenderer.start/create, not the manual worklet path |
| X5 | `AudioOfflineRenderer.start(source, optConfig, progress, abortSignal?, sampleRate?)` → `Promise<AudioBuffer>` | nuanced | dist:studio-core/dist/AudioOfflineRenderer.d.ts — signature exact, **but the whole namespace is `/** @deprecated */`** (since studio-core@0.0.93, commit 23a39af4 "adds deprecated"). CLAUDE.md presents it as a recommended shortcut with no deprecation note | Add deprecation note to CLAUDE.md + documentation/10-export.md; audioExport.ts migration is a follow-up (see D6) |
| X6 | `OfflineEngineRenderer.start(source, optConfig, progress, abortSignal?, sampleRate?)` → `Promise<AudioData>`; `.create(source, optConfig, sampleRate?)`; `.render(config, start, end, progress, abortSignal?)` | nuanced | dist:studio-core/dist/OfflineEngineRenderer.d.ts — shapes exact, but `progress` is `DefaultObservableValue<number>` (NOT `Progress.Handler` like AudioOfflineRenderer), `render`'s config is `OfflineEngineRenderConfig` (not ExportConfiguration), and `play()` returns `Promise<void>` (async since 0.0.139) | Note the progress-type difference in CLAUDE.md |
| X7 | `project.copy()` shares sampleManager but NOT engine preferences | confirmed | dist:studio-core/dist/project/Project.js:`copy(env) { return Project.load({...this.#env, ...env}, this.toArrayBuffer()) }` — env (incl. sampleManager) shared; preferences live on EngineWorklet (`get preferences(): PreferencesHost<EngineSettings>`), not in ProjectEnv | None |
| X8 | Engine worklet has 2 outputs; `connect(destination, 0)` | confirmed | dist:studio-core/dist/EngineWorklet.js:57-58 — `numberOfOutputs: 2, outputChannelCount: [numberOfChannels, 8]` | None |
| X9 | `WavFile.encodeFloats` duck-typed input | confirmed | dist:lib-dsp/dist/wav-file.d.ts:5-19 — `AudioData \| AudioBufferLike {sampleRate, length, numberOfChannels, getChannelData}`, plus optional `maxLength` | None |
| X10 | `TransferRegions.transfer(region, targetTrack, insertPosition, deleteSource?)` → `AnyRegionBox` | confirmed | dist:studio-adapters/dist/transfer/TransferRegions.d.ts | None |
| X11 | `TransferAudioUnits.transfer(boxes, skeleton, options)` with `{insertIndex?, deleteSource?, includeAux?, includeBus?, excludeTimeline?}` | confirmed | dist:studio-adapters/dist/transfer/TransferAudioUnits.d.ts | None |
| X12 | PresetEncoder/PresetDecoder/PresetHeader surface (encode, encodeEffects, decode, replaceAudioUnit, insertEffectChain, peekHasTimeline, ChainKind, MAGIC_HEADER_OPEN, FORMAT_VERSION) | confirmed | dist:studio-adapters/dist/preset/*.d.ts — all signatures match incl. `Attempt<void, string>` returns | None |
| X13 | Metronome gain `z.number().min(-Infinity).max(0)`, default −6 | confirmed | dist:studio-adapters/dist/engine/EnginePreferencesSchema.js:7-15 — also has undocumented `monophonic: boolean` field | None (optionally mention monophonic) |
| X14 | Mutate-copy-restore: copy is synchronous; "Modification only prohibited in transaction mode" | partially verified | `copy()` is synchronous (X7). The exact error string + "no audio blocks process in between" reasoning not re-verified (behavioral) | UNVERIFIED detail — leave as-is |

## 3. Demo-code findings

| # | Finding | Verdict | Evidence | Action |
|---|---------|---------|----------|--------|
| D1 | `project.lastRegionAction()` (export-demo.tsx:132) | confirmed — no drift | dist:studio-core/dist/project/Project.d.ts:85 `lastRegionAction(): ppqn`; Project.js:392 — returns `max(region.complete)` over the last region of every track (region end in ppqn), 0 when empty. Demo's max-bar derivation is correct | None |
| D2 | Master volume slider max +12 vs `decibel(-96,-9,+6)` (effects-demo.tsx:453) | nuanced — boosts past schema | Box constraints are **no-ops**: dist:lib-box/dist/constraints.js — `clampInt32`/`clampFloat32` bodies fully commented out (TODO). dist:studio-adapters/dist/AutomatableParameterFieldAdapter.js:152 — `getValue()` returns raw field value (out-of-bounds warn commented out at :90). @opendaw/studio-core@0.0.152:…/ChannelStripProcessor.ts:75 — `dbToGain(raw)` applied directly. So `volume.setValue(12)` stores 12 and renders ~3.98× gain — it really boosts to +12 | Fix task: cap slider at +6 (UI/automation unit-mapping and any future migration assume −96..+6), or explicitly annotate the out-of-schema boost |
| D3 | `genBox.index.setValue(0)` to move generator before showcase effect (werkstatt-demo.tsx:193) | **wrong idiom** | `insertEffect(field, factory, insertIndex)` exists (ProjectApi.d.ts:52) and routes through `IndexedBox.insertOrder` (dist:lib-box/dist/indexed-box.js:5-14) which **renumbers** existing devices. Raw `index.setValue(0)` leaves an already-loaded showcase effect ALSO at index 0 → duplicate index, ambiguous effect order in `audioEffects.adapters()` (sorted by index). Repro path: load showcase effect, then switch source to sine/noise | Fix task: replace the two-transaction insert+setValue with `project.api.insertEffect(field, EffectFactories.Werkstatt, 0)` |
| D4 | werkstatt-demo region init (lines 317-334): mute / waveformOffset / position / duration / loopDuration / loopOffset | confirmed correct | @opendaw/studio-boxes@0.0.94:…/AudioRegionBox.ts — `waveform-offset` float32 unit **"seconds"** (demo writes `PPQN.pulsesToSeconds(...)` ✓); `position` int32 ppqn (demo writes 0 ✓); `duration`/`loop-duration` float32 "mixed" = PPQN in Musical timeBase (demo writes BAR*16 ✓); `loop-offset` 0 ✓; `mute` boolean ✓ | None |
| D5 | rangeExport.ts stem export sets `useInstrumentOutput: true` (line 198) | **bug** (latent) | Same evidence as X3 — bypasses effects, sends, and channel strip; contradicts the adjacent `includeAudioEffects: true`. Audibly identical today only because the export demo inserts no effects and strips are at defaults; channel-strip volume/pan/mute would be silently ignored if ever non-default | Fix task: drop `useInstrumentOutput` (or set false) in rangeExport.ts |
| D6 | audioExport.ts / useAudioExport.ts use `AudioOfflineRenderer.start` | nuanced | Deprecated since studio-core@0.0.93 (still functional at 0.0.152; impl is the same project.copy + OfflineAudioContext + createEngine pattern as rangeExport.renderRange) | Fix task: note deprecation; migrating effects-demo export to rangeExport-style helper is a reasonable follow-up |
| D7 | audioExport.ts:174 — `{stems: stemsConfig}` where local `StemExportConfig` lacks required `useInstrumentOutput` | confirmed latent TS error | `npx tsc --noEmit --ignoreDeprecations "6.0"` → `src/lib/audioExport.ts(174,49): error TS2322` (pre-existing on main; Vite build doesn't run tsc). Runtime is FINE — undefined → falsy → channel-strip output, which is exactly what openDAW's own dialog ships (it also omits the flag) | Fix task: add `useInstrumentOutput: false` to `StemExportConfig` (or type the field optional to match SDK dialog usage via cast) — do NOT set true (see X3) |
| D8 | useAudioExport.ts:146 — `unit.input.label.unwrap() \|\| fallback` | nuanced | `label` is an Option; `.unwrap()` throws when empty — the `\|\|` only covers empty string. Upstream uses the identical idiom (ProjectDialogs.tsx:96), so severity low | Optional: `unwrapOrNull() ?? fallback` in fix task |
| D9 | useDynamicEffect.ts — all 10 effect configs: field names + ranges vs adapters/schemas | confirmed (3 minor nuances) | Verified per adapter+schema at tags: Reverb (wet/decay/preDelay/damp ✓, preDelay in seconds ✓), Compressor ✓, Delay (delayMusical index 0-20 ✓, hard-coded fraction list matches adapter ✓), Crusher (bits 1-16 ✓, boost 0-24 dB ✓ — the "NOT 0-1" comment is right; crush labeling correct), StereoTool (`stereo`/`panning` ✓), Revamp EQ (lowBell/midBell/highBell + gain ±24, freq 20-20k, q 0.01-10 ✓), Fold (drive 0-30, volume −18..0, overSampling {length:3} ✓), Dattorro ✓, Tidal (rate index 0-16 ✓, offset ±180° ✓), Maximizer (lookahead ✓). Nuances: (a) Dattorro **dry slider max +6 dB** exceeds the `decibel` (≤0) constraint and DefaultDecibel mapping — boosts unclamped, same mechanism as D2; (b) Maximizer slider min −30 matches box schema but exceeds the adapter UI mapping (−24..0); (c) Reverb preDelay slider min 0 is below the schema's exponential min 0.001 s (harmless, no clamp) | Fix task: clamp Dattorro dry to ≤0 (or document); the Maximizer/Reverb edges are cosmetic |
| D10 | effectPresets.ts — every preset value vs SDK ranges | confirmed | All 76 preset entries checked against adapter mappings/schemas: every value in range (Compressor ratio ≤20 < 24, release ≥30 ≥ 5, Crusher boost ≤16 ≤ 24, Delay feedback ≤0.7 ≤1, Dattorro dry ≤0, Maximizer ≥−15 ≥ −30, etc.) | None |
| D11 | werkstattScripts.ts generator scripts: `block.flags & 4` check + manual output zeroing | confirmed | Matches verified BlockFlag.playing=4 (E6) and no-clear behavior (E7) | None |
| D12 | rangeExport.ts render pipeline (createFor, createEngine, connect(dest,0), worklet preferences, setPosition/isReady/play/queryLoadingComplete) | confirmed | dist:studio-core/dist/AudioWorklets.d.ts:11-20; EngineWorklet.d.ts:11,13,30,34,35; matches deprecated AudioOfflineRenderer.js internals step-for-step | None |
| D13 | Pre-existing tsc errors in wave-6 files | inventory | effects-demo.tsx (TS2322 ×3, TS6133, TS2345), werkstatt-demo.tsx (TS6133 ×2 line 295), useDynamicEffect.ts (TS2339 'enabled' on never at 431 — narrowing artifact of assigning effectRef.current inside the modify callback; TS7006; TS2352 at 483), audioExport.ts (D7). All pre-exist on main | Fix tasks: judge "zero new errors" against this baseline; cheap ones (TS6133) can be cleaned in restyle commits |

## 4. Changelog sweep (0.0.135 → 0.0.154) — effects/scriptable/export relevance

| Change | Version | Classification |
|--------|---------|----------------|
| `ExportConfiguration` rename + `{stems, range}` shape | 0.0.147 | Already migrated (PR-era). No action |
| `ExportConfiguration.range` first-class section rendering | 0.0.147 | **Follow-up (doc)**: only honored by `OfflineEngineRenderer` (X4); rangeExport's manual createEngine path cannot adopt it without switching renderer (which re-introduces the liveStreamReceiver conflict the file exists to avoid). Document the limitation in export CLAUDE.md |
| `OfflineEngineRenderer.play()` now async | 0.0.139 | **Cheap improvement (doc)**: export CLAUDE.md's step-by-step list should show `play(): Promise<void>` |
| `Project.loadScriptDevices()` | 0.0.147 | **Follow-up**: relevant if a werkstatt demo ever restores compiled scripts from a saved project (replaces per-device `compiler.load`). Demo currently compiles fresh — irrelevant today |
| `WavFile.encodeInts16(audio, maxLength?)` | 0.0.147 | **Follow-up**: optional 16-bit WAV download in export demo. Not needed for fix tasks |
| `IndexedBox.moveIndices(field, startIndices, dropIndex)` | 0.0.147 | **Cheap improvement**: backs the D3 fix (though `insertEffect(..., 0)` is the simpler correct call) |
| `ProjectApi.compactTracks` | 0.0.147 | Irrelevant (recording-domain) |
| Preset system (`PresetStorage`, reworked PresetEncoder/Decoder binary layout) | 0.0.147 | Irrelevant to demos; export CLAUDE.md's PresetEncoder/Decoder signatures verified current (X12) |
| `EnginePreferencesSchema.recording.olderTakeScope` "none"; VaryingTempoMap memoisation; `TempoGridCursor` | 0.0.150 | Irrelevant / transparent (rangeExport's `intervalToSeconds` callers benefit automatically) |
| `metronome.monophonic` schema field | (present at 0.0.154) | **Cheap improvement (doc)**: add to metronome preference docs if touched in fix tasks |
| inputLatency pipeline, ProjectMetaBox, RemoteSelections, Colors palette | 0.0.154 | Irrelevant to wave 6 (recording / P2P / app-UI) |
| `AudioOfflineRenderer` `@deprecated` | long-standing (studio-core@0.0.93, predates audit range) | **Cheap improvement (doc)** + follow-up code migration (D6/X5) |

## 5. Unverified

- **X14 detail**: the exact error string "Modification only prohibited in transaction mode" and the
  "no audio blocks process between mutate and restore" reasoning — not re-verified this wave
  (would need a runtime repro; the synchronous-copy mechanism it rests on IS verified).
- **E23**: 0.0.129 display-name history ("Cheap Reverb" → "Free Reverb", Tone3000,
  AudioNamed alphabetical ordering) — historical claims, not re-verified against old tags.
- **Stem channel interleave order** ([stem1_L, stem1_R, stem2_L, …] matching `Object.keys(stems)`
  order): code-verified (EngineProcessor.ts:401-404 iterates `#stemExports` built from
  `Object.keys(stems)` in order at :337-340) but not runtime-verified that V8 key order survives
  the structured-clone into the worklet — extremely likely, and the demos' per-stem labels depend
  on it. Tried: traced `EngineProcessorAttachment` → worklet processorOptions; no contrary
  evidence found.
